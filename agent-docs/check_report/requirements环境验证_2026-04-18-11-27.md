# requirements 环境验证

- 时间：2026-04-18 11:27 +08
- 环境：`ai4story`
- 平台：macOS arm64

## 执行结果

- `conda env list`：确认新增环境 `ai4story`
- `conda run -n ai4story python --version`：`Python 3.11.15`
- `zsh -ic 'conda --version && conda activate ai4story && python --version'`：新 shell 中 `conda` 可用，激活后仍为 `Python 3.11.15`
- `conda run -n ai4story python -c "import rembg, onnxruntime, PIL, requests, tqdm, vtracer; print('imports ok')"`：在正常用户权限下输出 `imports ok`

## 备注

- 受限沙箱内导入 `rembg` 可能失败，因为 `numba` 需要写缓存目录；这不代表用户终端里的环境不可用。

## Changelog

- 2026-04-18 11:27 +08：完成环境验证记录。
