# 前端运行/构建时使用的后端地址。
#
# 本地开发（pnpm dev）— 留空，走 vite.config.ts 里的 /api /outputs /assets 代理：
#     VITE_API_BASE=
#
# 服务器构建（pnpm build 给前后端分离部署）— 直连后端 https：
#     VITE_API_BASE=https://api.your-domain.com
#
# 不要把 OSS 相关 secret 放在前端 env 里 — 全部由后端通过 storage backend 处理。
VITE_API_BASE=
